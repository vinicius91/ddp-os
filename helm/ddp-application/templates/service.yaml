apiVersion: v1
kind: Service
metadata:
  name: {{ include "ddp-application.fullname" . }}
  labels:
    {{- include "ddp-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ddp-application.selectorLabels" . | nindent 4 }}
