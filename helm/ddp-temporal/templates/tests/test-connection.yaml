apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ddp-temporal.fullname" . }}-test-connection"
  labels:
    {{- include "ddp-temporal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ddp-temporal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
