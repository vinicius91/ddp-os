apiVersion: v1
kind: Service
metadata:
  name: {{ include "ddp-temporal.fullname" . }}
  labels:
    {{- include "ddp-temporal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ddp-temporal.selectorLabels" . | nindent 4 }}
