apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ddp-kafka.fullname" . }}-test-connection"
  labels:
    {{- include "ddp-kafka.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ddp-kafka.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
