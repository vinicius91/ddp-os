apiVersion: v1
kind: Service
metadata:
  name: {{ include "ddp-kafka.fullname" . }}
  labels:
    {{- include "ddp-kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ddp-kafka.selectorLabels" . | nindent 4 }}
