apiVersion: v1
kind: Service
metadata:
  name: {{ include "ddp-flink.fullname" . }}
  labels:
    {{- include "ddp-flink.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ddp-flink.selectorLabels" . | nindent 4 }}
