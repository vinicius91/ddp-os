apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ddp-flink.fullname" . }}-test-connection"
  labels:
    {{- include "ddp-flink.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ddp-flink.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
